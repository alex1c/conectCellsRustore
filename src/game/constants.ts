/**
 * Re-export baseline aliases + schema constants.
 * Prefer `state.rules` / `getRulesForPreset` for actual gameplay.
 */

export {
	BOARD_SIZE,
	CHAIN_STEP_DELAY_MS,
	DEFAULT_RULE_PRESET,
	MAX_INITIAL_VALUE,
	MIN_CELL_VALUE,
	SAVE_SCHEMA_VERSION,
	SCORE_BASE,
	SPAWN_VALUE,
} from './rules'
