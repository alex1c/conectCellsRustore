/**
 * Shared surface / chrome colors for a calmer default look.
 * Not a dark theme — slightly less white glare for long OPPO sessions.
 * Board geometry and cell value palette stay independent.
 */

/** App / screen backdrop (Home, Game, Settings, How to Play). */
export const COLOR_SURFACE = '#d8e0eb'

/** Slightly deeper surface for cards sitting on COLOR_SURFACE. */
export const COLOR_SURFACE_CARD = '#eef2f7'

/** Soft board plane under empty hexes. */
export const COLOR_BOARD_PLANE = 'rgba(198, 210, 226, 0.72)'

/** Primary text on surface. */
export const COLOR_TEXT = '#0f172a'

/** Secondary / muted text. */
export const COLOR_TEXT_MUTED = '#526277'

/** Primary action blue (unchanged brand accent). */
export const COLOR_ACCENT = '#1d4ed8'

/** Secondary / restart button. */
export const COLOR_ACCENT_SECONDARY = '#334155'
